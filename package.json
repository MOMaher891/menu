{
  "name": "qr-menu-dashboard",
  "version": "1.0.0",
  "description": "Digital QR Menu & Admin Dashboard System",
  "main": "server.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix server --ignore-scripts && npm install --prefix client --ignore-scripts",
    "postinstall": "npm install --prefix server --ignore-scripts && npm install --prefix client --ignore-scripts",
    "seed": "node server/seed.js",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "npm run build --prefix client",
    "start": "node server/server.js"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
